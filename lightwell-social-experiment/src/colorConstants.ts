import { createTheme } from "@mui/material"

// export const TwitterBlue = `rgba(29, 161, 242, 0)`

export const theme = createTheme({
    palette: {
        primary: {
            main: '#1DA1F2',
        }
    }
})